# Fullstack_python
API - applicatio program interface
Middleware- stands as security check for request and response
Authorization vs authentication
authentication tells who are you eg: normal login authorization tells who are able to do eg:staff and studets
python -m venv venv
.env/bin/activate --github codespace
pip install flask
pip freeze>requirements.txt
pip install -r requirements.txt